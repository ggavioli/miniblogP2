<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
      <%= f.label :autor_name %><br>
      <%= f.text_field :autor_name %>
  </p>
  <p>
      <%= f.label :content %><br>
      <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>